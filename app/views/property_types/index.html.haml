%h1 Listing property_types

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @property_types.each do |property_type|
    %tr
      %td= property_type.name
      %td= link_to 'Show', property_type
      %td= link_to 'Edit', edit_property_type_path(property_type)
      %td= link_to 'Destroy', property_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Property type', new_property_type_path
